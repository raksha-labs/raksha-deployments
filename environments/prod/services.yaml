version: 1
environment: prod
services:
  portal-backend:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  portal:
    image_tag: prod-approved
    desired_count: 2
    strategy: blue_green
    config_profile: prod

  admin:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  rule-control-api:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  # Runtime services — baseline 2 tasks; ECS target-tracking autoscaling
  # scales up on CPU + queue-depth metrics. Do NOT set floor below 2 in
  # prod: lease coordination needs at least one survivor during deploys.
  engine:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  alert-control-api:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  notifier-runtime:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  stream-control-api:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  ingestion-gateway:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod

  simlab-api:
    image_tag: prod-approved
    desired_count: 2
    strategy: rolling
    config_profile: prod
